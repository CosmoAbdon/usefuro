# web-inspector

Inspector SPA (React + Vite + Tailwind), embedded into `furo` (client) via `go:embed`. Served at `localhost:4040`.

Implemented in milestone M4. Request list (SSE), detail view, replay, clear.
